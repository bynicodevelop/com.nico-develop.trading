export enum OrderSide {
	Buy = 'buy',
	Sell = 'sell',
}

export enum OrderType {
	Market = 'market',
	// Limit = 'limit',
	// Stop = 'stop',
	// StopLimit = 'stop_limit',
	// TrailingStop = 'trailing_stop',
}
